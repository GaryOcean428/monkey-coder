{
  "$schema": "https://schema.railpack.com",
  "version": "1",
  "metadata": {
    "name": "monkey-coder-frontend",
    "description": "Next.js frontend with static export"
  },
  "build": {
    "provider": "node",
    "packages": {
      "node": "20"
    },
    "steps": {
      "install": {
        "commands": [
          "corepack enable",
          "corepack prepare yarn@4.9.2 --activate",
          "yarn install --frozen-lockfile",
          "cd ../../packages/web && yarn install --frozen-lockfile",
          "echo '✅ Frontend dependencies installed'"
        ]
      },
      "build": {
        "commands": [
          "echo '🔨 Building Next.js frontend...'",
          "cd ../../packages/web && yarn build",
          "cd ../../packages/web && NEXT_OUTPUT_EXPORT=true yarn export",
          "test -d ../../packages/web/out && echo '✅ Static export complete' || echo '⚠️ Export failed'"
        ]
      }
    },
    "env": {
      "NODE_ENV": "production",
      "NEXT_OUTPUT_EXPORT": "true",
      "NEXT_TELEMETRY_DISABLED": "1",
      "NEXT_PUBLIC_API_URL": "https://${{monkey-coder-backend.RAILWAY_PUBLIC_DOMAIN}}"
    }
  },
  "deploy": {
    "startCommand": "npx serve -s ../../packages/web/out -p $PORT",
    "healthCheckPath": "/index.html",
    "healthCheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}