"""Top-level pytest configuration to host pytest_plugins (required after deprecation).

This file exists to satisfy pytest's requirement that pytest_plugins be defined only at the root
if used. We keep it minimal for now; add shared fixtures here when needed.
"""

# Placeholder for future plugin declarations
pytest_plugins = []
