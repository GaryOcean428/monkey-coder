{
  "$schema": "https://schema.railpack.com",
  "version": "1",
  "metadata": {
    "name": "monkey-coder-frontend",
    "description": "Next.js static export frontend service"
  },
  "build": {
    "provider": "node",
    "packages": {
      "node": "20"
    },
    "cache": {
      "paths": [
        "node_modules",
        ".yarn/cache",
        "packages/web/.next/cache",
        "packages/web/out"
      ]
    },
    "steps": {
      "install": {
        "commands": [
          "corepack enable",
          "corepack prepare yarn@4.9.2 --activate",
          "yarn install --immutable"
        ]
      },
      "build": {
        "commands": [
          "yarn workspace @monkey-coder/web build"
        ]
      }
    },
    "env": {
      "NODE_ENV": "production",
      "NEXT_OUTPUT_EXPORT": "true",
      "NEXT_TELEMETRY_DISABLED": "1"
    }
  },
  "deploy": {
    "startCommand": "npx serve@14.2.4 -s packages/web/out -l $PORT -c serve.json",
    "healthCheckPath": "/",
    "healthCheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
