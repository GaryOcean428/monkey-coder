{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "packages": {
    "python": "3.13",
    "sqlite3": "latest"
  },
  "build": {
    "command": "apt-get update && apt-get install -y sqlite3 libsqlite3-dev && pip install -r requirements.txt && cd packages/core && pip install -e ."
  },
  "deploy": {
    "startCommand": "python run_server.py"
  }
}