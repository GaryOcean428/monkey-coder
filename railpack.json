{
  "$schema": "https://schema.railpack.com",
  "version": "1",
  "build": {
    "provider": "python",
    "packages": {
      "python": "3.12",
      "node": "20"
    },
    "steps": {
      "install": {
        "commands": [
          "pip install --no-cache-dir --upgrade pip setuptools wheel",
          "pip install --no-cache-dir -r requirements-deploy.txt",
          "cd packages/core && pip install --no-cache-dir -e ."
        ]
      },
      "validate": {
        "commands": [
          "echo 'Testing imports for Railway deployment validation...'",
          "python -c 'import sys; sys.path.insert(0, \".\"); sys.path.insert(0, \"packages/core\"); from monkey_coder.app.main import app; print(\"✅ FastAPI app import successful\")'",
          "python -c 'import sys; sys.path.insert(0, \".\"); sys.path.insert(0, \"packages/core\"); from monkey_coder.app.main import HealthResponse; print(\"✅ Health response model import successful\")'"
        ],
        "inputs": [{ "step": "install" }]
      },
      "frontend": {
        "commands": [
          "corepack enable",
          "corepack prepare yarn@4.9.2 --activate",
          "yarn --version",
          "yarn install --immutable || yarn install",
          "yarn workspace @monkey-coder/web run export || echo 'Frontend build failed, API-only mode will be used'"
        ],
        "inputs": [{ "step": "validate" }]
      }
    }
  },
  "deploy": {
    "startCommand": "python run_server.py",
    "healthCheckPath": "/health",
    "healthCheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "inputs": [{ "step": "frontend" }]
  }
}
