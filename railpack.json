{
  "version": "1",
  "metadata": {
    "name": "monkey-coder-build-orchestrator",
    "description": "Yarn workspace build orchestration (Node.js 22 + Yarn 4.9.2) - not a Railway service"
  },
  "build": {
    "provider": "node",
    "packages": {
      "node": "22"
    },
    "steps": {
      "install": {
        "commands": [
          "corepack enable",
          "corepack prepare yarn@4.9.2 --activate",
          "yarn install --immutable",
          "if [ -f requirements-deploy.txt ]; then echo '✅ Found backend requirements-deploy.txt'; else echo '❌ Missing backend requirements-deploy.txt'; exit 1; fi",
          "cp requirements-deploy.txt services/backend/"
        ],
        "cache": {
          "paths": ["node_modules", ".yarn/cache"]
        }
      }
    },
    "env": {
      "NODE_ENV": "production"
    }
  }
}
